<% include ../partials/header %>

<div class="row">
  <div class="col-md-6 mx-auto">
    <div class="card">
      <div class="card-header text-center">
        <h3>Add Comment</h3>
      </div>
      <div class="card-body">
        <form action="/hotels/<%= hotel._id %>/comments/<%= comment.id %>?_method=PUT" method="POST">
          <div class="form-group">
            <textarea name="text" p rows="5" class="form-control"><%= comment.text %></textarea>
          </div>
          <button type="submit" class="btn btn-primary btn-block">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>

<% include ../partials/footer %>